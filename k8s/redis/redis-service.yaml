apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: ai
  labels:
    app: redis
    tier: cache
    env: local
spec:
  selector:
    app: redis
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
      name: redis
  type: ClusterIP
