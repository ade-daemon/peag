apiVersion: v1
kind: Service
metadata:
  name: open-webui
  namespace: ai
  labels:
    app: open-webui
    tier: frontend
    env: local
spec:
  selector:
    app: open-webui
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
      name: http
  type: ClusterIP
